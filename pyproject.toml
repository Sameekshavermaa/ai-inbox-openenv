[project]
name = "ai-inbox-openenv"
version = "0.1.0"
description = "Emotion-aware OpenEnv inbox environment"
authors = [{name = "Sameeksha Verma"}]

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests",
    "openenv>=0.1.13"
]

[project.scripts]
server = "app:app"